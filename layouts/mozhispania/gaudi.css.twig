body {
    background: url("/img/espana_logo.png");
    background-repeat: no-repeat;
    background-position: top left;
}

nav {
    display: flex; /*1*/
    flex-direction : row; /*2*/
    justify-content: flex-start; /*3*/
    background: #F1BF00;
}
 nav li {
    margin: 2px !important;
}

nav a {
    color: #AA151B;
    font-family: monospace;
    font-size: 1.1em;
    text-decoration: none;
    padding: 0 1em;
}

nav a:hover {
    color:white;
}

ol li::marker {
  color:red;
}