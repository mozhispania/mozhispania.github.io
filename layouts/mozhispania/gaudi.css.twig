nav {
    display: flex; /*1*/
    flex-direction : row; /*2*/
    justify-content: flex-start; /*3*/
    background: #222;
}
 nav li {
    flex:auto;
}

nav a {
    color: orange;
    font-family: monospace;
    font-size: 1.1em;
    text-decoration: none;
    padding: 0 1em;
}

nav a:hover {
    color:white;
}